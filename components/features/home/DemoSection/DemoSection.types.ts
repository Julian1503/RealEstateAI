/**
 * Props for the DemoSection component
 * Currently, this component doesn't require any props
 */
export interface DemoSectionProps {
  // No props needed at this time
}

/**
 * Props for the MockedVideoPreview component
 * Currently, this component doesn't require any props
 */
export interface MockedVideoPreviewProps {
  // No props needed at this time
}