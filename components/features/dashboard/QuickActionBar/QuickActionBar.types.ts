import {NavLinkItem} from "@shared/Header";

/**
 * Props for the QuickActionBar component
 * Currently, this component doesn't require any props
 */
export interface QuickActionBarProps {
  links: NavLinkItem[]
}