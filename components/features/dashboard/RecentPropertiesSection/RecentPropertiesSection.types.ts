import { Property } from '../PropertyCard/PropertyCard.types';

/**
 * Props for the RecentPropertiesSection component
 */
export interface RecentPropertiesSectionProps {
  /**
   * Array of properties to display in the section
   */
  properties: Property[];
}